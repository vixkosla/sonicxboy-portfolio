---
import '@fontsource-variable/tektur'
import '@fontsource/faster-one'
import '@fontsource/monoton'
import '@fontsource/rubik-mono-one'
import '@fontsource-variable/onest'
import '@fontsource-variable/jetbrains-mono'
import BrandMark from '../components/BrandMark.astro'
import HeroScene from '../components/HeroScene.tsx'
import { SITE, translations, type Lang } from '../i18n.ts'

interface Props {
  lang: Lang
}

const { lang } = Astro.props
const t = translations[lang]
const canonical = lang === 'ru' ? `${SITE}/` : `${SITE}/en/`

const personSchema = {
  '@context': 'https://schema.org',
  '@type': 'Person',
  name: 'SonicXBoy',
  jobTitle: 'WebGL Developer and Interactive Product Creator',
  url: `${SITE}/`,
  description: t.jsonLdDescription,
  knowsAbout: [
    'Three.js',
    'WebGL',
    'WebGPU',
    'GLSL',
    'React Three Fiber',
    'Mapbox',
    'React',
    'TypeScript',
    'Next.js',
    'Astro',
    'Node.js',
    'Browser Extensions',
    'Chrome Extensions',
    'Manifest V3',
    'Interactive 3D Graphics',
    '3D Product Configurators',
    'Data Visualization',
    'WebGL Performance Optimization',
    'Performance Engineering',
    'AI-assisted Software Development',
    'GSAP',
    'Blender',
  ],
  knowsLanguage: ['ru', 'en'],
  alumniOf: [
    {
      '@type': 'CollegeOrUniversity',
      name: 'МГТУ им. Н. Э. Баумана',
    },
    {
      '@type': 'EducationalOrganization',
      name: 'Школа 21',
    },
  ],
  homeLocation: {
    '@type': 'City',
    name: lang === 'ru' ? 'Санкт-Петербург' : 'Saint Petersburg',
  },
  sameAs: [
    'https://x.com/vixkosla',
    'https://www.upwork.com/freelancers/askerovt',
    'https://t.me/vixkosla',
    'https://github.com/vixkosla',
  ],
}

const websiteSchema = {
  '@context': 'https://schema.org',
  '@type': 'WebSite',
  name: 'SonicXBoy',
  alternateName: 'SonicXBoy WebGL Developer',
  url: `${SITE}/`,
}
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, viewport-fit=cover"
    />
    <title>{t.title}</title>
    <meta name="description" content={t.description} />
    <meta name="theme-color" content="#050907" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="canonical" href={canonical} />
    <link rel="alternate" hreflang="ru" href={`${SITE}/`} />
    <link rel="alternate" hreflang="en" href={`${SITE}/en/`} />
    <link rel="alternate" hreflang="x-default" href={`${SITE}/`} />
    <meta property="og:type" content="website" />
    <meta property="og:title" content={t.title} />
    <meta property="og:description" content={t.ogDescription} />
    <meta property="og:url" content={canonical} />
    <meta property="og:locale" content={lang === 'ru' ? 'ru_RU' : 'en_US'} />
    <meta
      property="og:locale:alternate"
      content={lang === 'ru' ? 'en_US' : 'ru_RU'}
    />
    <meta property="og:image" content={`${SITE}/og.png`} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content="SonicXBoy — WebGL Developer" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:site" content="@vixkosla" />
    <script type="application/ld+json" set:html={JSON.stringify(personSchema)} />
    <script type="application/ld+json" set:html={JSON.stringify(websiteSchema)} />
    <style>
      /* :global — the reactor card is React-rendered (no astro-cid),
         a scoped reset never reaches it. */
      :global(*),
      :global(*::before),
      :global(*::after) {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      :root {
        --ink: #e8f5ee;
        --ink-dim: rgba(218, 238, 229, 0.72);
        --reactor: #18d383;
        --reactor-mint: #6cf3b3;
        --emerald: var(--reactor);
        --emerald-dim: rgba(24, 211, 131, 0.32);
        --bg: #050907;
        --card-h: clamp(300px, 27.5vh, 360px);
        --font-display: 'Tektur Variable', system-ui, sans-serif;
        --font-slab: 'Rubik Mono One', 'Tektur Variable', system-ui, sans-serif;
        --font-body: 'Onest Variable', system-ui, sans-serif;
        --font-mono: 'JetBrains Mono Variable', ui-monospace, SFMono-Regular, Menlo, monospace;
        --safe-top: env(safe-area-inset-top, 0px);
        --safe-right: env(safe-area-inset-right, 0px);
        --safe-bottom: env(safe-area-inset-bottom, 0px);
        --safe-left: env(safe-area-inset-left, 0px);
      }

      html,
      body {
        width: 100%;
        max-width: 100%;
        min-width: 0;
      }

      body {
        font-family: var(--font-body);
        color: var(--ink);
        background: var(--bg);
        overflow-x: hidden;
        overflow-x: clip;
      }

      a:focus-visible {
        outline: 2px solid var(--emerald);
        outline-offset: 3px;
      }

      :global(.hero-scene) {
        position: fixed;
        inset: 0;
        z-index: 0;
        width: 100%;
        max-width: 100%;
      }

      /* ── Wordmark ─────────────────────────────────────────── */

      .brand {
        position: fixed;
        top: 1.6rem;
        left: clamp(1.5rem, 8vw, 9rem);
        z-index: 2;
        font-size: 0.72rem;
      }

      /* ── Language switcher ────────────────────────────────── */

      .lang {
        position: fixed;
        top: 1.9rem;
        right: clamp(1.5rem, 8vw, 9rem);
        z-index: 2;
        display: flex;
        align-items: center;
        gap: 0.45rem;
        font-family: var(--font-mono);
        font-size: 0.74rem;
        font-weight: 600;
        letter-spacing: 0.12em;
      }

      .lang__link {
        color: var(--ink-dim);
        text-decoration: none;
        padding: 0.2rem 0.1rem;
        pointer-events: auto;
        transition: color 0.2s ease;
      }

      .lang__link:hover {
        color: var(--ink);
      }

      .lang__link.is-active {
        color: var(--emerald);
      }

      .lang__sep {
        color: rgba(232, 245, 238, 0.25);
      }

      /* ── Reactor card (arrives from the 3D scene) ─────────── */

      :global(.reactor-card) {
        position: fixed;
        left: 0;
        right: 0;
        bottom: 0;
        z-index: 3;
        width: 100vw;
        min-height: var(--card-h);
        display: flex;
        flex-direction: column;
        padding:
          1.4rem clamp(1.5rem, 8vw, 9rem)
          1.25rem;
        overflow: hidden;
        isolation: isolate;
        color: #eafff5;
        background:
          radial-gradient(circle at 8% 118%, rgba(43, 111, 67, 0.22), transparent 34%),
          radial-gradient(circle at 92% -20%, rgba(24, 211, 131, 0.08), transparent 28%),
          linear-gradient(135deg, rgba(8, 34, 24, 0.92), rgba(3, 13, 9, 0.97)),
          rgba(5, 9, 7, 0.94);
        border: 1px solid rgba(24, 211, 131, 0.54);
        border-right: 0;
        border-bottom: 0;
        border-left: 0;
        border-radius: 0;
        box-shadow:
          0 24px 70px rgba(0, 0, 0, 0.44),
          inset 0 0 34px rgba(24, 211, 131, 0.055);
        backdrop-filter: blur(16px) saturate(125%);
        opacity: 0;
        filter: blur(7px);
        transform: translate3d(-100%, -2.8rem, 0) scale(0.18, 0.38);
        transform-origin: left center;
        transition:
          transform 1.16s cubic-bezier(0.16, 1, 0.3, 1),
          opacity 0.18s ease-out,
          filter 0.72s ease-out;
        pointer-events: none;
        will-change: transform, opacity, filter;
      }

      :global(.reactor-card::after) {
        content: '';
        position: absolute;
        inset: 0;
        z-index: 0;
        pointer-events: none;
        background:
          linear-gradient(90deg, rgba(255, 96, 72, 0.08), transparent 32%),
          radial-gradient(circle at 92% -20%, rgba(24, 211, 131, 0.07), transparent 28%),
          radial-gradient(
              circle at 9px 9px,
              rgba(232, 197, 111, 0.052) 0 0.7px,
              transparent 0.95px
            )
            0 0 / 18px 18px,
          repeating-linear-gradient(
            90deg,
            transparent 0,
            transparent 71px,
            rgba(24, 211, 131, 0.045) 71px,
            rgba(24, 211, 131, 0.045) 72px
          ),
          repeating-linear-gradient(
            0deg,
            transparent 0,
            transparent 71px,
            rgba(24, 211, 131, 0.04) 71px,
            rgba(24, 211, 131, 0.04) 72px
          ),
          repeating-linear-gradient(
            90deg,
            transparent 0,
            transparent 17px,
            rgba(24, 211, 131, 0.022) 17px,
            rgba(24, 211, 131, 0.022) 18px
          ),
          repeating-linear-gradient(
            0deg,
            transparent 0,
            transparent 17px,
            rgba(24, 211, 131, 0.019) 17px,
            rgba(24, 211, 131, 0.019) 18px
          );
        opacity: 0;
        clip-path: polygon(0 0, 0 0, 0 100%, 0 100%);
      }

      :global(.reactor-card.is-visible::after) {
        animation: reactor-board-etch 1.85s cubic-bezier(0.16, 1, 0.3, 1) 0.52s both;
      }

      :global(.reactor-card__circuit) {
        position: absolute;
        inset: 0;
        z-index: 1;
        width: 100%;
        height: 100%;
        overflow: visible;
        pointer-events: none;
      }

      :global(.reactor-card__circuit-frame) {
        fill: none;
        stroke: rgba(232, 197, 111, 0.075);
        stroke-width: 0.7;
        stroke-dasharray: 2 11;
        vector-effect: non-scaling-stroke;
      }

      :global(.reactor-card__circuit-grooves path) {
        fill: none;
        stroke: rgba(0, 5, 3, 0.64);
        stroke-width: 3.1;
        stroke-linecap: square;
        stroke-linejoin: miter;
        stroke-dasharray: 1;
        stroke-dashoffset: 1;
        vector-effect: non-scaling-stroke;
        opacity: 0;
      }

      :global(.reactor-card__circuit-metal path) {
        fill: none;
        stroke: rgba(232, 197, 111, 0.3);
        stroke-width: 0.86;
        stroke-linecap: square;
        stroke-linejoin: miter;
        stroke-dasharray: 1;
        stroke-dashoffset: 1;
        vector-effect: non-scaling-stroke;
        filter: drop-shadow(0 0 2px rgba(232, 197, 111, 0.16));
        opacity: 0;
      }

      :global(.reactor-card__circuit-metal path:nth-child(4n + 2)) {
        stroke: rgba(243, 218, 154, 0.22);
        stroke-width: 0.66;
      }

      :global(.reactor-card__circuit-grooves path:nth-child(3n)) {
        stroke-width: 2.5;
      }

      :global(.reactor-card__circuit-pads rect) {
        fill: rgba(4, 20, 12, 0.92);
        stroke: rgba(232, 197, 111, 0.34);
        stroke-width: 0.72;
        vector-effect: non-scaling-stroke;
        opacity: 0;
      }

      :global(.reactor-card.is-visible .reactor-card__circuit-metal path) {
        animation: reactor-circuit-draw 1.05s cubic-bezier(0.16, 1, 0.3, 1)
          calc(0.14s + var(--circuit-delay, 0s)) both;
      }

      :global(.reactor-card.is-visible .reactor-card__circuit-grooves path) {
        animation: reactor-circuit-settle 0.9s cubic-bezier(0.16, 1, 0.3, 1)
          calc(0.5s + var(--circuit-delay, 0s)) both;
      }

      :global(.reactor-card.is-visible .reactor-card__circuit-pads rect) {
        animation: reactor-circuit-pad 0.42s ease-out
          calc(0.72s + var(--pad-delay, 0s)) both;
      }

      :global(.reactor-card.is-visible) {
        opacity: 1;
        filter: blur(0);
        transform: translate3d(0, 0, 0) scale(1);
        animation: reactor-card-arrival 1.8s ease-out both;
        pointer-events: auto;
      }

      :global(.reactor-card__content) {
        position: relative;
        z-index: 2;
        display: flex;
        flex: 1;
        flex-direction: column;
        width: 100%;
        min-width: 0;
      }

      :global(.reactor-card__signal) {
        position: absolute;
        inset: 0 auto 0 0;
        z-index: 3;
        width: 3px;
        color: #ff6048;
        background: currentColor;
        box-shadow:
          0 0 12px currentColor,
          0 0 34px currentColor;
      }

      :global(.reactor-card.is-visible .reactor-card__signal) {
        animation: reactor-signal-settle 2.4s ease-out both;
      }

      :global(.reactor-card__signal::after) {
        content: '';
        position: absolute;
        left: 8px;
        top: 15px;
        width: 6px;
        height: 6px;
        border-radius: 50%;
        background: currentColor;
        box-shadow: 0 0 14px currentColor;
        animation: reactor-signal 0.8s ease-in-out 3;
      }

      :global(.reactor-card__meta) {
        position: relative;
        z-index: 1;
        display: flex;
        justify-content: space-between;
        gap: 1rem;
        margin-bottom: 0.72rem;
        color: rgba(108, 243, 179, 0.72);
        font: 600 0.66rem/1.2 var(--font-mono);
        letter-spacing: 0.14em;
        text-shadow: 0 1px 8px rgba(2, 10, 7, 0.9);
      }

      :global(.reactor-card h2) {
        position: relative;
        z-index: 1;
        margin: 0 0 0.85rem;
        font-family: var(--font-slab);
        font-size: clamp(0.95rem, 1.32vw, 1.28rem);
        font-weight: 400;
        letter-spacing: 0.02em;
        text-transform: uppercase;
        text-shadow: 0 1px 10px rgba(2, 10, 7, 0.92);
      }

      /* Three glyph slabs: uppercase Rubik Mono One, both edges
         justified, one shared top rule so the blocks read as three
         stone tablets hanging from the same line. */
      :global(.reactor-card__body) {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: clamp(1.5rem, 2.6vw, 3.25rem);
        align-items: start;
        width: 100%;
      }

      :global(.reactor-card p) {
        position: relative;
        z-index: 1;
        color: rgba(224, 243, 234, 0.8);
        border-top: 1px solid rgba(24, 211, 131, 0.34);
        padding-top: 0.72rem;
        font-family: var(--font-slab);
        font-size: clamp(0.55rem, 0.62vw, 0.72rem);
        line-height: 1.62;
        letter-spacing: 0.01em;
        text-transform: uppercase;
        text-align: justify;
        hyphens: auto;
        text-shadow: 0 1px 8px rgba(2, 10, 7, 0.94);
      }

      :global(.reactor-card p + p) {
        margin-top: 0;
      }

      :global(.reactor-card__stack) {
        position: relative;
        z-index: 1;
        display: flex;
        gap: 0.85rem;
        margin-top: 1rem;
        color: var(--reactor);
        font: 600 0.65rem/1 var(--font-mono);
        letter-spacing: 0.12em;
      }

      @keyframes reactor-card-arrival {
        0%, 18% {
          border-color: rgba(255, 96, 72, 0.68);
          box-shadow:
            0 0 28px rgba(255, 96, 72, 0.32),
            0 24px 70px rgba(0, 0, 0, 0.44);
        }
        100% {
          border-color: rgba(24, 211, 131, 0.48);
          box-shadow:
            0 24px 70px rgba(0, 0, 0, 0.44),
            inset 0 0 34px rgba(24, 211, 131, 0.055);
        }
      }

      @keyframes reactor-signal {
        0%, 100% { opacity: 0.36; transform: scale(0.82); }
        50% { opacity: 1; transform: scale(1.18); }
      }

      @keyframes reactor-signal-settle {
        0%, 64% { color: #ff6048; }
        100% { color: var(--reactor); }
      }

      @keyframes reactor-board-etch {
        0% {
          opacity: 0;
          clip-path: polygon(0 0, 0 0, 0 100%, 0 100%);
        }
        42% {
          opacity: 0.72;
          clip-path: polygon(0 0, 58% 0, 40% 100%, 0 100%);
        }
        100% {
          opacity: 0.82;
          clip-path: polygon(0 0, 100% 0, 100% 100%, 0 100%);
        }
      }

      @keyframes reactor-circuit-draw {
        0% {
          opacity: 0;
          stroke-dashoffset: 1;
        }
        28% { opacity: 1; }
        100% {
          opacity: 1;
          stroke-dashoffset: 0;
        }
      }

      @keyframes reactor-circuit-settle {
        0% {
          opacity: 0;
          stroke-dashoffset: 1;
        }
        100% {
          opacity: 0.82;
          stroke-dashoffset: 0;
        }
      }

      @keyframes reactor-circuit-pad {
        0% { opacity: 0; }
        60% { opacity: 0.72; }
        100% { opacity: 1; }
      }

      /* ── Hero overlay ─────────────────────────────────────── */

      main {
        position: relative;
        z-index: 1;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        justify-content: center;
        text-align: left;
        /* Top padding keeps the eyebrow clear of the fixed brand badge */
        padding: 8.5rem clamp(1.5rem, 8vw, 9rem) 2rem;
        pointer-events: none;
        transition: padding-bottom 1.1s cubic-bezier(0.16, 1, 0.3, 1);
      }

      /* Make room for the arriving card instead of hiding content */
      :global(body.reactor-card-visible) main {
        padding-bottom: calc(var(--card-h) + 2.5rem);
      }

      .eyebrow {
        font-family: var(--font-mono);
        font-size: 0.72rem;
        font-weight: 500;
        letter-spacing: 0.22em;
        text-transform: uppercase;
        color: var(--emerald);
        margin-bottom: 1.1rem;
      }

      h1 {
        /* Monoton and Faster One are single-weight: keep 400 so the
           line-art glyphs don't get faux-bolded. */
        font-family: 'Faster One', var(--font-display);
        /* vw-clamp for width, vh cap so the two-line block doesn't
           swallow shorter screens (1080p) while 2K keeps full size */
        font-size: min(clamp(2.3rem, 8.6vw, 9rem), 11vh);
        font-weight: 400;
        line-height: 1;
        letter-spacing: 0;
        text-transform: uppercase;
        margin-bottom: 1.6rem;
      }

      .headline__solid {
        font-family: 'Monoton', 'Faster One', var(--font-display);
        /* 1.6em width-matches the 5-glyph Monoton line to the
           9-glyph Faster One line below it. */
        font-size: 1.6em;
        color: var(--reactor);
        background: linear-gradient(
          165deg,
          #eafff5 0%,
          #78aa94 16%,
          #173c2d 35%,
          #071b13 48%,
          #129d65 62%,
          #74edb5 79%,
          #0b5036 100%
        );
        background-size: 100% 128%;
        background-position: 50% 44%;
        background-clip: text;
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        filter: drop-shadow(0 0 0.18em rgba(24, 211, 131, 0.13));
      }

      :global(body[data-material-baseline]) .headline__solid {
        color: var(--ink);
        background: none;
        -webkit-text-fill-color: currentColor;
        filter: none;
      }

      .headline__solid,
      .headline__outline {
        display: block;
        position: relative;
        width: max-content;
        isolation: isolate;
      }

      .headline__outline {
        color: var(--emerald);
      }

      .headline__outline::after {
        content: attr(data-text);
        position: absolute;
        inset: 0;
        z-index: 1;
        color: transparent;
        background: linear-gradient(
          108deg,
          transparent 0 29%,
          var(--orbit-wave-glow) 36%,
          var(--orbit-wave-color) 44%,
          var(--orbit-wave-hot) 51%,
          var(--orbit-wave-color) 58%,
          var(--orbit-wave-glow) 66%,
          transparent 73% 100%
        );
        background-size: 214% 214%;
        background-position: -58% 148%;
        background-clip: text;
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        -webkit-text-stroke: 0;
        opacity: 0;
        filter: drop-shadow(0 0 0 transparent);
        pointer-events: none;
        will-change: background-position, opacity, filter;
      }

      :global(body[data-orbit-title-wave='outer-approach']) .headline__outline::after {
        --orbit-wave-color: #38e8ad;
        --orbit-wave-hot: #effff9;
        --orbit-wave-glow: rgba(56, 232, 173, 0.34);
        animation: orbit-title-wave-approach 2.28s cubic-bezier(0.24, 0, 0.18, 1) both;
      }

      :global(body[data-orbit-title-wave='outer-near']) .headline__outline::after {
        --orbit-wave-color: #1fcde0;
        --orbit-wave-hot: #e8fdff;
        --orbit-wave-glow: rgba(31, 205, 224, 0.36);
        animation: orbit-title-wave-near 2.36s cubic-bezier(0.24, 0, 0.18, 1) both;
      }

      :global(body[data-orbit-title-wave='outer-return']) .headline__outline::after {
        --orbit-wave-color: #4167ff;
        --orbit-wave-hot: #e7edff;
        --orbit-wave-glow: rgba(65, 103, 255, 0.32);
        animation: orbit-title-wave-return 1.58s cubic-bezier(0.24, 0, 0.18, 1) both;
      }

      @keyframes orbit-title-wave-approach {
        0% { opacity: 0; background-position: -62% 152%; }
        10% { opacity: 1; }
        80% { opacity: 1; }
        100% {
          opacity: 0;
          background-position: 148% -50%;
          filter: drop-shadow(0 0 18px rgba(56, 232, 173, 0.38));
        }
      }

      @keyframes orbit-title-wave-near {
        0% { opacity: 0; background-position: -62% 152%; }
        10% { opacity: 1; }
        80% { opacity: 1; }
        100% {
          opacity: 0;
          background-position: 148% -50%;
          filter: drop-shadow(0 0 19px rgba(31, 205, 224, 0.4));
        }
      }

      @keyframes orbit-title-wave-return {
        0% { opacity: 0; background-position: -62% 152%; }
        10% { opacity: 1; }
        68% { opacity: 1; }
        100% {
          opacity: 0;
          background-position: 148% -50%;
          filter: drop-shadow(0 0 20px rgba(65, 103, 255, 0.4));
        }
      }

      .subtitle {
        max-width: 46ch;
        font-size: 1.08rem;
        line-height: 1.6;
        color: var(--ink-dim);
      }

      .tech-stack {
        margin-top: 1.9rem;
        display: flex;
        gap: 0.55rem;
        flex-wrap: wrap;
        max-width: 40rem;
      }

      .tech-badge {
        padding: 0.38rem 0.7rem;
        background: rgba(24, 211, 131, 0.08);
        border: 1px solid var(--emerald-dim);
        border-radius: 2px;
        font-family: var(--font-mono);
        font-size: 0.68rem;
        font-weight: 500;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        color: var(--emerald);
      }

      /* ── Air-support oval ─────────────────────────────────────
         One ellipse line repeated in vertical storeys, so the lanes
         stand as walls of an elliptical cylinder; crossings emerge
         only in the viewer's projection. Each pulse fades in, glides
         one lap with ease-in-out, then fades out — the figure reads
         as loosely filled, appearing and disappearing in places.
         Width matches the measured headline width (75vh on desktop);
         the wall is raised so its upper storeys pass behind the
         subtitle and badges. Hidden until the first orbit title
         wave ignites it; fades with the card. */
      .hero-oval {
        position: absolute;
        left: clamp(1.5rem, 8vw, 9rem);
        bottom: clamp(0.75rem, 9vh, 6rem);
        width: min(66rem, 75vh);
        aspect-ratio: 1200 / 360;
        pointer-events: none;
        opacity: 0;
        transition: opacity 1.6s ease;
      }

      :global(body[data-oval-on]) .hero-oval {
        opacity: var(--oval-visibility, 1);
      }

      :global(body.reactor-card-visible) .hero-oval {
        opacity: 0;
      }

      .oval-pulse {
        fill: none;
        stroke-linecap: round;
        animation:
          hero-oval-lap var(--dur) cubic-bezier(0.37, 0, 0.63, 1)
          var(--delay) infinite;
      }

      .oval-pulse--rev {
        animation-name: hero-oval-lap-rev;
      }

      .oval-halo {
        stroke: rgba(24, 211, 131, 0.14);
        stroke-width: 9;
        stroke-dasharray: 20 80;
      }

      .oval-body {
        stroke: var(--lane-color, #25f09e);
        stroke-width: 2.6;
        stroke-dasharray: 13 87;
      }

      .oval-body--echo {
        stroke-dasharray: 9 91;
        opacity: 0.8;
      }

      /* White-hot filament rides the leading edge of the body dash,
         the same core+halo recipe the grid cage uses. */
      .oval-hot {
        stroke: #e2fff3;
        stroke-width: 1.6;
        stroke-dasharray: 6 94;
      }

      @keyframes hero-oval-lap {
        0% { stroke-dashoffset: 0; opacity: 0; }
        7% { opacity: var(--peak, 1); }
        76% { opacity: var(--peak, 1); }
        100% { stroke-dashoffset: -100; opacity: 0; }
      }

      @keyframes hero-oval-lap-rev {
        0% { stroke-dashoffset: 0; opacity: 0; }
        7% { opacity: var(--peak, 1); }
        76% { opacity: var(--peak, 1); }
        100% { stroke-dashoffset: 100; opacity: 0; }
      }

      /* ── Card socials & contact (right column of the card) ── */

      :global(.reactor-card__actions) {
        position: relative;
        z-index: 2;
        align-self: end;
        display: flex;
        align-items: center;
        justify-content: flex-end;
        gap: 0.6rem;
        margin-top: auto;
        padding-top: 0.9rem;
        visibility: hidden;
        pointer-events: auto;
      }

      :global(.reactor-card.is-visible .reactor-card__actions) {
        visibility: visible;
      }

      :global(.reactor-card__icon) {
        display: grid;
        place-items: center;
        width: 2.75rem;
        height: 2.75rem;
        color: var(--ink);
        background: rgba(24, 211, 131, 0.06);
        border: 1px solid var(--emerald-dim);
        border-radius: 2px;
        transition:
          border-color 0.2s ease,
          color 0.2s ease,
          background 0.2s ease,
          transform 0.2s ease;
      }

      :global(.reactor-card__icon:hover) {
        color: var(--emerald);
        border-color: var(--emerald);
        background: rgba(24, 211, 131, 0.08);
        transform: translateY(-2px);
      }

      :global(.reactor-card__icon svg) {
        width: 1.15rem;
        height: 1.15rem;
        fill: currentColor;
      }

      :global(.reactor-card__cta) {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 0.55rem;
        height: 2.75rem;
        padding: 0 1.15rem;
        margin-left: 0.4rem;
        font-family: var(--font-mono);
        font-size: 0.74rem;
        font-weight: 600;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        text-decoration: none;
        color: #04140c;
        background: var(--emerald);
        border: 1px solid var(--emerald);
        border-radius: 2px;
        transition: filter 0.2s ease, transform 0.2s ease;
      }

      :global(.reactor-card__cta:hover) {
        filter: brightness(1.14);
        transform: translateY(-2px);
      }

      @media (max-width: 1180px) {
        :global(.reactor-card) {
          min-height: clamp(390px, 48vh, 520px);
        }

        :global(.reactor-card__circuit) {
          opacity: 0.78;
        }

        :global(.reactor-card__body) {
          grid-template-columns: repeat(2, minmax(0, 1fr));
        }

        :global(.reactor-card__body p:last-child) {
          grid-column: 1 / -1;
        }

        :global(.reactor-card p) {
          font-size: clamp(0.55rem, 0.86vw, 0.68rem);
        }
      }

      @media (prefers-reduced-motion: reduce) {
        .headline__outline::after {
          animation: none !important;
          opacity: 0 !important;
        }

        :global(.reactor-card__icon),
        :global(.reactor-card__cta),
        :global(.reactor-card::after),
        :global(.reactor-card__circuit-grooves path),
        :global(.reactor-card__circuit-metal path),
        :global(.reactor-card__circuit-pads rect) {
          animation: none !important;
          transition: none;
        }
        :global(.reactor-card::after) {
          opacity: 0.82;
          clip-path: none;
        }
        :global(.reactor-card__circuit-grooves path),
        :global(.reactor-card__circuit-metal path) {
          opacity: 1;
          stroke-dashoffset: 0;
        }
        :global(.reactor-card__circuit-pads rect) {
          opacity: 1;
        }
        :global(.reactor-card__icon:hover),
        :global(.reactor-card__cta:hover) {
          transform: none;
        }
      }

      @media (max-width: 720px) {
        /* Mobile staging, act 1: the headline owns the top, the scene owns
           the middle on its own, a thin subtitle + chip strip sits at the
           bottom. Act 2: the reactor card covers the strip as a bottom
           sheet; the strip fades under it. */
        main {
          width: 100%;
          height: 100dvh;
          min-height: 100svh;
          justify-content: flex-start;
          padding:
            calc(var(--safe-top) + 7.6rem)
            calc(var(--safe-right) + 1.25rem)
            calc(var(--safe-bottom) + 0.9rem)
            calc(var(--safe-left) + 1.25rem);
        }

        :global(body.reactor-card-visible) main {
          padding-bottom: 0.9rem;
        }

        .brand {
          top: calc(var(--safe-top) + 1.1rem);
          left: calc(var(--safe-left) + 1.25rem);
        }

        .lang {
          top: calc(var(--safe-top) + 1.2rem);
          right: calc(var(--safe-right) + 1.25rem);
        }

        .eyebrow {
          font-size: clamp(0.56rem, 2.5vw, 0.62rem);
          letter-spacing: 0.18em;
          margin-bottom: 0.7rem;
          white-space: nowrap;
        }

        h1 {
          --mobile-title-size: min(12.4vw, 5.6svh);
          font-size: var(--mobile-title-size);
          margin-bottom: 0;
        }

        .subtitle {
          margin-top: auto;
          max-width: 100%;
          font-size: 0.95rem;
          line-height: 1.5;
        }

        .tech-stack {
          align-self: stretch;
          width: 100%;
          min-width: 0;
          margin-top: 0.75rem;
          max-width: 100%;
          gap: 0.45rem;
          flex-wrap: nowrap;
          overflow-x: auto;
          padding-bottom: 0.35rem;
          pointer-events: auto;
          -webkit-overflow-scrolling: touch;
          overscroll-behavior-inline: contain;
          scrollbar-width: none;
          mask-image: linear-gradient(90deg, #000 0, #000 86%, transparent 100%);
          -webkit-mask-image: linear-gradient(90deg, #000 0, #000 86%, transparent 100%);
        }

        .tech-stack::-webkit-scrollbar {
          display: none;
        }

        .tech-badge {
          flex-shrink: 0;
        }

        /* The oval anchors directly under the headline block (same offset
           math as the reactor card top), its upper storeys tucked behind
           the headline's lower edge. Width matches the mobile headline
           (~83vw). */
        .hero-oval {
          --oval-visibility: 0.5;
          left: calc(var(--safe-left) + 1.25rem);
          bottom: auto;
          top: calc(
            var(--safe-top) + 7.6rem + 0.82rem + 0.7rem +
            min(32.24vw, 14.56svh) + 0.4rem
          );
          width: 84vw;
        }

        .subtitle,
        .tech-stack {
          transition: opacity 0.4s ease;
        }

        :global(body.reactor-card-visible) .subtitle,
        :global(body.reactor-card-visible) .tech-stack {
          opacity: 0;
        }

        :global(.reactor-card) {
          top: calc(
            var(--safe-top) + 7.6rem + 0.82rem + 0.7rem +
              min(32.24vw, 14.56svh) + 1rem
          );
          left: 0;
          right: 0;
          bottom: 0;
          width: 100%;
          height: auto;
          min-height: 0;
          max-height: none;
          padding:
            1rem
            calc(var(--safe-right) + 1rem)
            calc(var(--safe-bottom) + 1rem)
            calc(var(--safe-left) + 1rem);
          overflow-y: auto;
          overscroll-behavior: contain;
        }

        :global(.reactor-card__circuit) {
          opacity: 0.58;
        }

        :global(.reactor-card__body) {
          grid-template-columns: 1fr;
          gap: 0.68rem;
        }

        :global(.reactor-card__body p:last-child) {
          grid-column: auto;
        }

        :global(.reactor-card__actions) {
          align-self: stretch;
          justify-content: flex-start;
          flex-wrap: nowrap;
          gap: 0.45rem;
          padding-top: 0.85rem;
        }

        :global(.reactor-card__icon) {
          flex: 0 0 2.5rem;
          width: 2.5rem;
          height: 2.5rem;
        }

        :global(.reactor-card__cta) {
          flex: 1 1 auto;
          min-width: 0;
          height: 2.5rem;
          margin-left: 0;
          padding: 0 0.65rem;
          font-size: 0.64rem;
          letter-spacing: 0.075em;
          white-space: nowrap;
        }

        :global(.reactor-card__meta) {
          margin-bottom: 0.6rem;
          font-size: 0.6rem;
        }

        :global(.reactor-card h2) {
          margin-bottom: 0.62rem;
          font-size: 0.88rem;
        }

        :global(.reactor-card p) {
          font-size: 0.58rem;
          line-height: 1.58;
        }
      }

      /* Short landscape screens are a separate composition: identity stays
         in the left rail, WebGL owns the middle/right, and the final
         information surface becomes a scrollable right-hand panel. */
      @media (max-width: 1180px) and (max-height: 800px) and (orientation: landscape) {
        main {
          width: 44%;
          height: 100dvh;
          min-height: 0;
          justify-content: center;
          padding:
            calc(var(--safe-top) + 4.7rem)
            0.75rem
            calc(var(--safe-bottom) + 0.8rem)
            calc(var(--safe-left) + 1rem);
        }

        .brand {
          top: calc(var(--safe-top) + 0.65rem);
          left: calc(var(--safe-left) + 1rem);
          font-size: 0.56rem;
        }

        .lang {
          top: calc(var(--safe-top) + 0.8rem);
          right: calc(var(--safe-right) + 1rem);
        }

        .eyebrow {
          margin-bottom: 0.55rem;
          font-size: 0.57rem;
          letter-spacing: 0.16em;
          white-space: nowrap;
        }

        h1 {
          font-size: min(6vw, 10.5svh);
          margin-bottom: 0;
        }

        .subtitle {
          margin-top: 0.85rem;
          max-width: 34ch;
          font-size: 0.8rem;
          line-height: 1.38;
        }

        .tech-stack {
          display: none;
        }

        :global(body.reactor-card-visible) main {
          padding-bottom: calc(var(--safe-bottom) + 0.8rem);
        }

        :global(.reactor-card) {
          top: 0;
          right: 0;
          bottom: 0;
          left: 44%;
          width: auto;
          height: 100dvh;
          min-height: 0;
          max-height: none;
          padding:
            calc(var(--safe-top) + 0.85rem)
            calc(var(--safe-right) + 1rem)
            calc(var(--safe-bottom) + 0.85rem)
            1rem;
          overflow-y: auto;
          overscroll-behavior: contain;
          border-top: 0;
          border-left: 1px solid rgba(24, 211, 131, 0.54);
        }

        :global(.reactor-card__meta) {
          margin-bottom: 0.5rem;
          font-size: 0.57rem;
        }

        :global(.reactor-card h2) {
          margin-bottom: 0.55rem;
          font-size: 0.82rem;
        }

        :global(.reactor-card__body) {
          grid-template-columns: 1fr;
          gap: 0.58rem;
        }

        :global(.reactor-card__body p:last-child) {
          grid-column: auto;
        }

        :global(.reactor-card p) {
          font-size: 0.56rem;
          line-height: 1.54;
        }

        :global(.reactor-card__actions) {
          align-self: stretch;
          justify-content: flex-start;
          flex-wrap: nowrap;
          gap: 0.4rem;
          padding-top: 0.72rem;
        }

        :global(.reactor-card__icon) {
          flex: 0 0 2.35rem;
          width: 2.35rem;
          height: 2.35rem;
        }

        :global(.reactor-card__cta) {
          flex: 1 1 auto;
          min-width: 0;
          height: 2.35rem;
          margin-left: 0;
          padding: 0 0.6rem;
          font-size: 0.59rem;
          letter-spacing: 0.065em;
          white-space: nowrap;
        }
      }
    </style>
  </head>
  <body>
    <HeroScene client:load copy={t.card} />

    <header class="brand">
      <BrandMark />
    </header>

    <nav class="lang" aria-label={t.ariaLang}>
      <a
        class:list={['lang__link', { 'is-active': lang === 'ru' }]}
        href="/"
        aria-current={lang === 'ru' ? 'page' : undefined}>RU</a
      >
      <span class="lang__sep" aria-hidden="true">/</span>
      <a
        class:list={['lang__link', { 'is-active': lang === 'en' }]}
        href="/en/"
        aria-current={lang === 'en' ? 'page' : undefined}>EN</a
      >
    </nav>

    <main>
      {/* Air-support oval under the headline + stack: one ellipse line
          repeats in vertical storeys, so the lanes stand as walls of an
          elliptical cylinder — crossings emerge only in the viewer's
          projection. Greens grade from deep at the base to pale at the
          top rim. Ignites with the first orbit title wave; pure CSS. */}
      <svg class="hero-oval" viewBox="0 0 1200 360" aria-hidden="true">
        <g style="--dur:6.8s;--delay:-1.2s;--peak:0.62;--lane-color:#0f6e4a">
          <path class="oval-pulse oval-halo" pathLength="100" d="M 40 260 A 560 70 0 1 1 1160 260 A 560 70 0 1 1 40 260 Z" />
          <path class="oval-pulse oval-body" pathLength="100" d="M 40 260 A 560 70 0 1 1 1160 260 A 560 70 0 1 1 40 260 Z" />
          <path class="oval-pulse oval-hot" pathLength="100" d="M 40 260 A 560 70 0 1 1 1160 260 A 560 70 0 1 1 40 260 Z" />
          <path class="oval-pulse oval-pulse--rev oval-body oval-body--echo" style="--dur:7.4s;--delay:-5.1s" pathLength="100" d="M 40 260 A 560 70 0 1 1 1160 260 A 560 70 0 1 1 40 260 Z" />
        </g>
        <g style="--dur:8.2s;--delay:-4.6s;--peak:0.78;--lane-color:#0e8f5a">
          <path class="oval-pulse oval-pulse--rev oval-halo" pathLength="100" d="M 40 233 A 560 70 0 1 1 1160 233 A 560 70 0 1 1 40 233 Z" />
          <path class="oval-pulse oval-pulse--rev oval-body" pathLength="100" d="M 40 233 A 560 70 0 1 1 1160 233 A 560 70 0 1 1 40 233 Z" />
          <path class="oval-pulse oval-pulse--rev oval-hot" pathLength="100" d="M 40 233 A 560 70 0 1 1 1160 233 A 560 70 0 1 1 40 233 Z" />
          <path class="oval-pulse oval-body oval-body--echo" style="--dur:6.1s;--delay:-0.9s" pathLength="100" d="M 40 233 A 560 70 0 1 1 1160 233 A 560 70 0 1 1 40 233 Z" />
        </g>
        <g style="--dur:5.9s;--delay:-2.3s;--peak:0.92;--lane-color:#14b874">
          <path class="oval-pulse oval-halo" pathLength="100" d="M 40 207 A 560 70 0 1 1 1160 207 A 560 70 0 1 1 40 207 Z" />
          <path class="oval-pulse oval-body" pathLength="100" d="M 40 207 A 560 70 0 1 1 1160 207 A 560 70 0 1 1 40 207 Z" />
          <path class="oval-pulse oval-hot" pathLength="100" d="M 40 207 A 560 70 0 1 1 1160 207 A 560 70 0 1 1 40 207 Z" />
          <path class="oval-pulse oval-pulse--rev oval-body oval-body--echo" style="--dur:8.6s;--delay:-6.2s" pathLength="100" d="M 40 207 A 560 70 0 1 1 1160 207 A 560 70 0 1 1 40 207 Z" />
        </g>
        <g style="--dur:7.5s;--delay:-5.8s;--peak:1;--lane-color:#18d383">
          <path class="oval-pulse oval-pulse--rev oval-halo" pathLength="100" d="M 40 180 A 560 70 0 1 1 1160 180 A 560 70 0 1 1 40 180 Z" />
          <path class="oval-pulse oval-pulse--rev oval-body" pathLength="100" d="M 40 180 A 560 70 0 1 1 1160 180 A 560 70 0 1 1 40 180 Z" />
          <path class="oval-pulse oval-pulse--rev oval-hot" pathLength="100" d="M 40 180 A 560 70 0 1 1 1160 180 A 560 70 0 1 1 40 180 Z" />
          <path class="oval-pulse oval-body oval-body--echo" style="--dur:5.7s;--delay:-2.6s" pathLength="100" d="M 40 180 A 560 70 0 1 1 1160 180 A 560 70 0 1 1 40 180 Z" />
        </g>
        <g style="--dur:6.2s;--delay:-0.7s;--peak:0.95;--lane-color:#25f09e">
          <path class="oval-pulse oval-halo" pathLength="100" d="M 40 153 A 560 70 0 1 1 1160 153 A 560 70 0 1 1 40 153 Z" />
          <path class="oval-pulse oval-body" pathLength="100" d="M 40 153 A 560 70 0 1 1 1160 153 A 560 70 0 1 1 40 153 Z" />
          <path class="oval-pulse oval-hot" pathLength="100" d="M 40 153 A 560 70 0 1 1 1160 153 A 560 70 0 1 1 40 153 Z" />
          <path class="oval-pulse oval-pulse--rev oval-body oval-body--echo" style="--dur:7.9s;--delay:-4.4s" pathLength="100" d="M 40 153 A 560 70 0 1 1 1160 153 A 560 70 0 1 1 40 153 Z" />
        </g>
        <g style="--dur:8.8s;--delay:-3.9s;--peak:0.82;--lane-color:#4be8b3">
          <path class="oval-pulse oval-pulse--rev oval-halo" pathLength="100" d="M 40 127 A 560 70 0 1 1 1160 127 A 560 70 0 1 1 40 127 Z" />
          <path class="oval-pulse oval-pulse--rev oval-body" pathLength="100" d="M 40 127 A 560 70 0 1 1 1160 127 A 560 70 0 1 1 40 127 Z" />
          <path class="oval-pulse oval-pulse--rev oval-hot" pathLength="100" d="M 40 127 A 560 70 0 1 1 1160 127 A 560 70 0 1 1 40 127 Z" />
          <path class="oval-pulse oval-body oval-body--echo" style="--dur:6.6s;--delay:-7.1s" pathLength="100" d="M 40 127 A 560 70 0 1 1 1160 127 A 560 70 0 1 1 40 127 Z" />
        </g>
        <g style="--dur:7.1s;--delay:-6.4s;--peak:0.72;--lane-color:#7dffc9">
          <path class="oval-pulse oval-halo" pathLength="100" d="M 40 100 A 560 70 0 1 1 1160 100 A 560 70 0 1 1 40 100 Z" />
          <path class="oval-pulse oval-body" pathLength="100" d="M 40 100 A 560 70 0 1 1 1160 100 A 560 70 0 1 1 40 100 Z" />
          <path class="oval-pulse oval-hot" pathLength="100" d="M 40 100 A 560 70 0 1 1 1160 100 A 560 70 0 1 1 40 100 Z" />
          <path class="oval-pulse oval-pulse--rev oval-body oval-body--echo" style="--dur:9.1s;--delay:-1.8s" pathLength="100" d="M 40 100 A 560 70 0 1 1 1160 100 A 560 70 0 1 1 40 100 Z" />
        </g>
      </svg>

      <p class="eyebrow">{t.eyebrow}</p>

      <h1>
        <span class="headline__solid">WebGL</span>
        <span class="headline__outline" data-text="Developer">Developer</span>
      </h1>

      <p class="subtitle">{t.subtitle}</p>

      <div class="tech-stack">
        <span class="tech-badge">Three.js</span>
        <span class="tech-badge">WebGL</span>
        <span class="tech-badge">GLSL</span>
        <span class="tech-badge">React Three Fiber</span>
        <span class="tech-badge">Mapbox</span>
        <span class="tech-badge">React</span>
        <span class="tech-badge">TypeScript</span>
        <span class="tech-badge">Next.js</span>
        <span class="tech-badge">Astro</span>
        <span class="tech-badge">Node.js</span>
        <span class="tech-badge">GSAP</span>
        <span class="tech-badge">Rapier</span>
      </div>
    </main>
  </body>
</html>
